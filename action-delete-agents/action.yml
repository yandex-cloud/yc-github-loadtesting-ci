name: 'Run test'
description: ''
runs:
  using: "composite"
  steps:
    - uses: ./action-setup
    - name: delete agents
      run: |
        set -e
        automation/agent_delete.sh --labels "workflow=${WORKFLOW_ID}"
      shell: bash